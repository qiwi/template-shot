import {TemplateShot} from '../index';
import {expect} from 'chai';

describe('test suite', function(): void{
    // TODO: add tests
    it('dummy', function(done: MochaDone): void {
        expect(true).to.be.eql(true);
        done();
    });
});